{
  "buildCommand": "cd ../.. && pnpm install && cd apps/facilitator && pnpm build",
  "installCommand": "cd ../.. && pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}

