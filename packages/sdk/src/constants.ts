// Superfluid uses (365/12) days per month = 2,628,000 seconds
export const SECONDS_PER_MONTH = 2628000n;

// Default monthly amount: 1 USDCx (18 decimals)
export const DEFAULT_MONTHLY_AMOUNT = BigInt("1000000000000000000");

// Minimum underlying (USDC) balance to subscribe (6 decimals)
export const MIN_USDC_BALANCE = BigInt("2000000");
